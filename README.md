# Лабораторная работа №7: Адаптивная верстка с Flexbox

## Цель работы
Освоить современные технологии CSS-разметки — Flexbox для создания адаптивных веб-страниц.

## Что такое Flexbox?
Flexbox (Flexible Box Layout) — это модуль CSS для создания гибких одномерных раскладок. Он идеально подходит для:
- Выравнивания элементов по горизонтали или вертикали
- Распределения свободного пространства
- Создания навигационных меню
- Центрирования элементов

## Основные понятия
- **Flex-контейнер** — родительский элемент с `display: flex`
- **Flex-элементы** — дочерние элементы внутри контейнера

## Ход выполнения работы

### Шаг 1. Создание рабочей директории
### Шаг 2. Подключение к Git и GitHub
```bash
git init -b main
git add .
git commit -m "Initial commit"
# Создаём репозиторий на GitHub и подключаем
git remote add origin <URL_репозитория>
git push -u origin main
```

### Шаг 3 Изученные свойства Flexbox
Свойства flex-контейнера
Свойство	Значения	Описание
display	flex	Определяет flex-контейнер
flex-direction	row, column, row-reverse, column-reverse	Направление осей
justify-content	flex-start, flex-end, center, space-between, space-around, space-evenly	Выравнивание по главной оси
align-items	stretch, flex-start, flex-end, center, baseline	Выравнивание по поперечной оси
flex-wrap	nowrap, wrap, wrap-reverse	Перенос элементов
gap	любое значение в px, em, %	Отступы между элементами

### Шаг 4. Практические примеры
В работе были созданы три практических примера:

Горизонтальное навигационное меню — с использованием display: flex, justify-content: space-around

Карточки товаров — с использованием flex-wrap, gap, flex: 1 1 250px

Центрирование элемента — с использованием justify-content: center и align-items: center

### Вывод
В ходе лабораторной работы я освоил основные свойства **Flexbox** и научился создавать адаптивные макеты. **Flexbox** значительно упрощает процесс верстки и позволяет создавать гибкие, адаптивные интерфейсы с минимальным количеством кода.